{
  "hooks": {
    "PreToolUse": [
      {
        "matcher": "Read|Edit|Write",
        "hooks": [
          {
            "type": "command",
            "command": "FILE=$(jq -r '.tool_input.file_path // empty') && BASENAME=$(basename \"$FILE\") && if [ \"$BASENAME\" = \".env\" ]; then echo \"Blocked: reading .env is not allowed\" >&2; exit 2; fi; exit 0"
          }
        ]
      }
    ]
  }
}
